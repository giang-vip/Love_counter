<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="24dp"
    app:cardBackgroundColor="#FFFFFF"
    app:cardCornerRadius="20dp"
    app:cardElevation="8dp"
    app:strokeWidth="0dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Lịch chọn ngày hỗ trợ tiêu đề trắng, chữ đen, mũi tên hồng chuẩn thiết kế -->
        <com.applandeo.materialcalendarview.CalendarView
            android:id="@+id/calendarView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:headerColor="#FFFFFF"
            app:headerLabelColor="#20242A"
            app:previousButtonSrc="@drawable/ic_arrow_left_pink"
            app:forwardButtonSrc="@drawable/ic_arrow_right_pink"
            app:abbreviationsBarColor="#FFFFFF"
            app:abbreviationsLabelsColor="#888888"
            app:pagesColor="#FFFFFF"
            app:daysLabelsColor="#20242A"
            app:anotherMonthsDaysLabelsColor="#CCCCCC"
            app:selectionColor="#FF004F"
            app:selectionLabelColor="#FFFFFF"
            app:type="one_day_picker" />

        <!-- Hàng chứa 2 nút Cancel và Save -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:gravity="center"
            android:orientation="horizontal">

            <!-- Nút Cancel -->
            <androidx.appcompat.widget.AppCompatButton
                android:id="@+id/btnCancel"
                android:layout_width="0dp"
                android:layout_height="44dp"
                android:layout_marginEnd="8dp"
                android:layout_weight="1"
                android:background="@drawable/bg_button_cancel"
                android:text="@string/btn_cancel"
                android:textAllCaps="false"
                android:textColor="#20242A"
                android:textSize="14sp"
                android:textStyle="bold" />

            <!-- Nút Save -->
            <androidx.appcompat.widget.AppCompatButton
                android:id="@+id/btnSave"
                android:layout_width="0dp"
                android:layout_height="44dp"
                android:layout_marginStart="8dp"
                android:layout_weight="1"
                android:background="@drawable/bg_button_save"
                android:text="@string/btn_save"
                android:textAllCaps="false"
                android:textColor="#FFFFFF"
                android:textSize="14sp"
                android:textStyle="bold" />
        </LinearLayout>

    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
